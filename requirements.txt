# BARJ Volume Controller — Python dependencies
# Install with:  pip install -r requirements.txt

pyserial>=3.5
pyyaml>=6.0
pystray>=0.19.4
Pillow>=10.0.0

# Detects apps that are running but not currently producing sound. Optional:
# without it, apps only show as running while actively making sound.
psutil>=5.9.0

# Windows audio (Core Audio API)
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"

# Linux audio (PulseAudio / PipeWire-pulse)
pulsectl>=23.5.2; sys_platform == "linux"
